---
# maintains mainnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:
    starknet:
      hosts:
        ts.attestor.starknet.mainnet.encapsulate.xyz:
          type: attestor
        ts.node.starknet.mainnet.encapsulate.xyz:
          type: node
